{
  "devDependencies": {
    "solhint": "^5.0.3"
  },
  "scripts": {
    "lint": "npm run lint:fmt && npm run lint:sol",
    "lint:fmt": "forge fmt --check",
    "lint:fix": "forge fmt",
    "lint:sol": "node ./node_modules/solhint/solhint.js {src,test}/**/*.sol",
    "coverage": "forge coverage --report lcov && genhtml lcov.info --branch-coverage --ignore-errors corrupt --ignore-errors inconsistent --ignore-errors category --output-dir coverage",
    "deploy": "source .env && forge script script/01_DeployGBera.s.sol --sender $ETH_FROM --ledger --rpc-url $RPC_URL --broadcast"
  },
  "dependencies": {
    "solidity-ast": "^0.4.59"
  }
}
